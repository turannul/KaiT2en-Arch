post_install() {
    systemctl enable t2-fancontrol.service
    [ -d /run/systemd/system ] && systemctl start t2-fancontrol.service
}

post_upgrade() {
    [ -d /run/systemd/system ] && systemctl try-restart t2-fancontrol.service
}

pre_remove() {
    [ -d /run/systemd/system ] && systemctl stop t2-fancontrol.service
    systemctl disable t2-fancontrol.service
}
